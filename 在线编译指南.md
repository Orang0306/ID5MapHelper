# 📱 无需 Android Studio，3 步获取 APK

本项目已配置 GitHub Actions 自动编译，你只需要一个 GitHub 账号（免费），不需要安装任何开发工具。

---

## 第一步：创建 GitHub 仓库（2分钟）

1. 打开 https://github.com ，注册/登录账号
2. 右上角点 **+** → **New repository**
3. 填写：
   - Repository name: `ID5MapHelper`（随便起）
   - 选择 **Public**（公开，免费可用 Actions）
   - 勾选 **Add a README file**（随便勾一个，方便上传）
4. 点 **Create repository**

## 第二步：上传项目文件（1分钟）

1. 在仓库页面，点 **Add file** → **Upload files**
2. 把 `ID5MapHelper` 文件夹里的**所有文件和文件夹**拖进去（包括 `.github` 文件夹）
   - ⚠️ 注意：要上传文件夹里的**内容**，不是整个文件夹
   - 需要包含：`app/`、`gradle/`、`build.gradle`、`settings.gradle`、`.github/` 等所有文件
3. 拉到页面底部，点 **Commit changes**

## 第三步：下载编译好的 APK（3-5分钟）

1. 上传后，点仓库顶部的 **Actions** 标签
2. 会看到一个正在运行的 **Build APK** 任务，等待它变成绿色 ✅
3. 点进这个任务，拉到最底部 **Artifacts** 区域
4. 点击 **ID5MapHelper-debug** 下载 ZIP 压缩包
5. 解压得到 `app-debug.apk` 文件

## 安装到手机

1. 把 `app-debug.apk` 传到手机（微信/QQ/数据线都行）
2. 手机上点击 APK 文件安装
3. 如果提示"未知来源"，去设置里允许安装即可

---

## 💡 常见问题

**Q: Actions 里看不到任务？**
A: 确认上传了 `.github/workflows/build.yml` 文件，并且仓库是 Public 的。

**Q: 编译失败了怎么办？**
A: 点进失败的任务，看日志里的错误信息，把错误截图发给我排查。

**Q: 可以用私有仓库吗？**
A: 可以，但私有仓库每月有 2000 分钟免费额度，这个项目编译一次约 3 分钟，完全够用。

**Q: 以后修改了代码怎么更新？**
A: 重新上传修改后的文件，Actions 会自动重新编译，去下载新的 APK 即可。

---

## 🎯 编译成功后

安装 App 后：
1. 打开 App → 点「开启悬浮窗」→ 授权权限
2. 右下角 ➕ 添加你的地图
3. 切换到第五人格，点屏幕边缘的小球即可查看地图

详细使用说明见 [README.md](README.md)
